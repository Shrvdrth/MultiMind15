As a system, I want a budget mechanism so that each session has a controlled number of AI operations.

Acceptance Criteria
Each session has AI “credits”
Each agent call consumes credit
Block execution when credits exhausted