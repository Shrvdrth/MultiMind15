As a system, I want protected routes so unauthorized users cannot access restricted pages.

Acceptance Criteria
Auth middleware validates JWT
Protected frontend routes redirect unauthenticated users
Expired tokens rejected